using System;
using System.IO;

public class Class1
{
	public Auto()
	{
		string mark;
		string model;
		int cum;
	}

	for (int j = i + 1; j < model.Autos; j++)
	{
		if (model[i] < model[j])
		{
			temp = model[i];
			model[i] = model[j];
			model[j] = temp;
		}
	}

	for (int j = i + 1; j < cum.Autos; j++)
	{
		if (cum[i] < cum[j])
		{
			temp = cum[i];
			cum[i] = cum[j];
			cum[j] = temp;
		}
	}
	File.Create("d:\\a.txt");
}